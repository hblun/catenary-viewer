services:
  viewer:
    build:
      context: .
      dockerfile: Dockerfile.gtfs-viewer
    environment:
      GTFS_DIR: /gtfs
      OUTPUT_DIR: /app/site/data
      GTFS_FILE: ${GTFS_FILE:-}
    ports:
      - "8081:8081"
    volumes:
      - ${GTFS_HOST_DIR:-./gtfs}:/gtfs:ro
